---
level: "excellent"
summary: "static, 조건·반복문, GuessGame 구현과 Java 메모리 구조"
reviewedAt: "2026-08-12"
---

# 🤖 AI 학습 정리

2026.08.12

> 오늘의 TLP와 TIL을 바탕으로 오늘 학습을 한 번 정리해 봅니다.

## 종합

Java의 static, 조건·반복문, 입력·출력 흐름을 GuessGame 구현까지 이어가고 Stack, Heap, String Pool까지 확장해 정리했다.

## 잘 이어간 점

- Scanner, Math.random(), 조건문, 반복문, printf()를 입력 → 처리 → 출력 흐름으로 연결해 GuessGame 구조로 정리했다.
- 메서드 실행을 Thread, Execution Stack, Stack Frame, Heap 참조와 연결하고 String 생성 방식과 GC 관계까지 함께 남겼다.

## 다음에 다듬을 점

- static 변수·메서드와 인스턴스 변수·메서드의 실제 접근 관계를 더 복습할 부분으로 남겼다.
- Stack과 Heap의 관계를 객체 생성과 메서드 호출 코드로 직접 따라가며 다시 확인할 필요를 적었다.

## 다음 액션

- static 멤버와 인스턴스 멤버의 접근 가능 여부를 작은 클래스 예제로 다시 본다.
- 객체 생성과 메서드 호출 코드를 기준으로 Stack Frame, 참조값, Heap 객체의 흐름을 손으로 따라간다.
